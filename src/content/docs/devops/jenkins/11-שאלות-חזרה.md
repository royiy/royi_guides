---
title: "שאלות חזרה"
category: DevOps/Jenkins
part: 11/10
---

שאלות חזרה אינטראקטיביות על כל מה שלמדתם לאורך מדריכי ה-Jenkins (התקנה והגדרה ראשונית, Pipeline Declarative מול Scripted, דוגמאות Jenkinsfile מעשיות, ניהול Plugins, שילוב Git/GitHub ו-Webhooks, אינטגרציה עם Docker, אבטחה והרשאות ו-Credentials, ארכיטקטורת Master/Agent ו-Distributed Builds, ו-Groovy Shared Libraries). בחרו תשובה בכל שאלה ותקבלו מיד משוב האם היא נכונה, יחד עם הסבר.

<div id="quiz-score" class="quiz-score">ענית נכון על 0 מתוך 10</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>1. מהי התיקייה <code>JENKINS_HOME</code> ולמה חשוב לגבות אותה?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. תיקיית קבצי לוג זמניים בלבד, אפשר למחוק אותה בלי בעיה</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. התיקייה שמכילה את כל נתוני Jenkins - jobs, builds, plugins, config.xml, ו-credentials מוצפנים - ולכן חובה לגבות אותה באופן קבוע</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. תיקייה שמכילה רק את קובץ ההתקנה הראשוני של Jenkins</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. תיקיית הבינארי של Java שממנו Jenkins רץ</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> <code>JENKINS_HOME</code> מכיל את כל הנתונים של Jenkins - jobs, builds, plugins ותצורה, כולל credentials מוצפנים. גיבוי שוטף שלה הוא קריטי, ומעבר בין שרתים נעשה פשוט על ידי העתקת התיקייה כולה.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>2. מה ההבדל המרכזי בין Declarative Pipeline ל-Scripted Pipeline?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. Declarative הוא syntax מובנה עם structure קבוע (<code>pipeline { agent {} stages {} }</code>) שנבדק לפני ריצה (validation/linting), ואילו Scripted הוא Groovy מלא בתוך <code>node {}</code> שנבדק תוך כדי ריצה</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. Scripted Pipeline רץ רק על Windows, ו-Declarative רק על Linux</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Declarative Pipeline לא תומך כלל בהגדרת stages</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין הבדל מעשי, שתי הצורות מתבצעות באופן זהה לחלוטין</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Declarative קריא יותר ומומלץ לרוב המקרים, עם validation מובנה לפני ריצה, בעוד Scripted גמיש יותר אך מורכב יותר ונבדק רק תוך כדי ריצה. אפשר גם לשלב לוגיקת Scripted בתוך Declarative באמצעות בלוק <code>script {}</code>.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>3. מה עושה ה-step <code>retry(3) { ... }</code> ב-Jenkinsfile?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. מוחק את ה-build לצמיתות אחרי 3 ניסיונות כושלים</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. מריץ מחדש את הבלוק שבפנים עד 3 פעמים לפני שהוא נכשל סופית - שימושי לטיפול בקריאות רשת לא יציבות (flaky)</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. מריץ את הבלוק 3 פעמים במקביל על 3 agents שונים</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. ממתין 3 דקות לפני שהוא מריץ את הבלוק בפעם הראשונה</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> <code>retry(N) { ... }</code> מנסה שוב את הבלוק עד N פעמים לפני שהוא נכשל סופית - למשל לטיפול בקריאת <code>curl</code> לבדיקת health שלפעמים נכשלת באופן זמני בגלל תקלת רשת חולפת.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>4. מהו Jenkins Configuration as Code (JCasC) ולמה הוא שימושי?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. Plugin שמאפשר להגדיר את כל תצורת Jenkins (משתמשים, credentials, plugins, agents) בקובץ YAML, מה שהופך את ההגדרה לגרסתית (version-controlled), משוחזרת ואוטומטית</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. כלי שממיר אוטומטית כל Jenkinsfile Scripted ל-Declarative</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. פיצ'ר שמאפשר להריץ builds ישירות מתוך קובצי Word</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. Plugin שמנהל אך ורק הרשאות משתמשים ולא שום דבר אחר</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> JCasC מאפשר להגדיר את כל תצורת Jenkins - משתמשים, אבטחה, credentials, plugins - כקובץ YAML הנשמר ב-source control, כך שההגדרה הופכת לגרסתית, ניתנת לשחזור ואוטומטית במקום תלות בקליקים ידניים ב-UI.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>5. מה ההבדל בין הפעלת build באמצעות Webhook לעומת Polling SCM?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. Polling הוא push התראה מיידית מה-Git server, ו-Webhook הוא בדיקה תקופתית יזומה מצד Jenkins</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. Webhook הוא push התראה מה-Git server ל-Jenkins ברגע שקורה אירוע - מיידי ויעיל, אך דורש ש-Jenkins יהיה נגיש מהאינטרנט; Polling הוא בדיקה תקופתית יזומה מצד Jenkins - פחות יעיל ועם עיכוב, אך לא דורש נגישות מבחוץ</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. שתי השיטות זהות לגמרי מבחינה טכנית ואין ביניהן שום הבדל</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. Polling עובד רק עם GitLab, ו-Webhook עובד רק עם GitHub</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Webhook שולח התראה מיידית מה-Git server לכתובת כמו <code>http://your-jenkins-url/github-webhook/</code> ברגע שקורה push, ואילו Polling SCM (למשל עם schedule כמו <code>H/5 * * * *</code>) בודק תקופתית מ-Jenkins עצמו - שימושי כש-Jenkins נמצא מאחורי firewall ולא נגיש מבחוץ.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>6. מה ההבדל בין שיתוף <code>docker.sock</code> של ה-host לבין Docker-in-Docker (DinD) אמיתי?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. שיתוף ה-socket מריץ containers "אחים" ל-Jenkins על ה-host daemon עצמו (פשוט ומהיר, אך עם חשיפת גישה מלאה ל-daemon), ואילו DinD מריץ Docker daemon נפרד בתוך container עם בידוד טוב יותר, אך דורש <code>--privileged</code></button>
    <button class="quiz-option" type="button" data-choice="ב">ב. אין שום הבדל ביניהם, שתי השיטות דורשות תמיד privileged mode</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. DinD תמיד מהיר יותר ואין לו שום חיסרון אבטחתי</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. שיתוף ה-socket אפשרי רק על agents מבוססי Windows</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> שיתוף <code>/var/run/docker.sock</code> יוצר containers "אחים" ל-Jenkins על ה-host daemon - מהיר וקל אך מסוכן כי המשתמש בתוך Jenkins יכול לשלוט בכל ה-Docker daemon של ה-host. DinD אמיתי (למשל <code>docker:24-dind</code> עם <code>--privileged</code>) מספק בידוד טוב יותר, במחיר דרישת privileged mode ולעיתים בעיות ביצועים.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>7. מהו Groovy Sandbox ב-Jenkins ולמה הוא חשוב?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. סביבת בדיקה שמריצה כל pipeline פעמיים ברצף כדי לוודא עקביות</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. מנגנון הגנה שמריץ Pipeline/script בלוקים בסביבה מוגבלת שחוסמת קריאות מסוכנות ל-Java/Groovy APIs; פעולות שאינן ב-whitelist דורשות אישור מפורש ב-Script Approval</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. תוסף שמצפין אוטומטית את כל קבצי ה-Jenkinsfile בהיסטוריית Git</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. מנגנון שמאפשר להריץ אך ורק Declarative Pipelines, לעולם לא Scripted</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Pipelines שנטענים מ-source control רצים כברירת מחדל ב-Groovy Sandbox, שחוסם פעולות מסוכנות. אם script דורש הרשאה מיוחדת, מנהל צריך לאשר אותה מפורשות דרך Manage Jenkins → Script Approval - ומומלץ לא לאשר סקריפטים בעיוורון.</div>
</div>

<div class="quiz-card" data-answer="ג">
  <p class="quiz-question"><strong>8. מהו Executor ב-Jenkins?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. סוג של plugin ייעודי לניהול credentials בלבד</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. תגית (Label) שמסמנת אילו agents תומכים ב-Docker</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. "חריץ" (slot) על agent שיכול להריץ build אחד בו-זמנית; כמות ה-executors צריכה להתאים למשאבי החומרה כדי למנוע resource contention</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. השם הישן ל-Controller (Master), לפני שינוי המינוח בגרסאות עדכניות</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Executor הוא חריץ על agent שמריץ build אחד בו-זמנית. הגדרת יותר מדי executors על agent אחד שה-builds שלו כבדים ב-CPU/RAM עלולה לגרום ל-resource contention ותוצאות לא יציבות.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>9. מה ההבדל בין תיקיית <code>vars/</code> לתיקיית <code>src/</code> ב-Shared Library של Jenkins?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. <code>vars/</code> מכיל global variables/steps שנגישים ישירות מ-Jenkinsfile כפונקציות (כמו step מובנה), ו-<code>src/</code> מכיל Groovy classes מלאים שנטענים עם <code>import</code></button>
    <button class="quiz-option" type="button" data-choice="ב">ב. <code>vars/</code> מיועד אך ורק לקבצי YAML, ו-<code>src/</code> אך ורק לקבצי Groovy</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. <code>src/</code> נוצרת אוטומטית על ידי Jenkins ואסור לערוך אותה ידנית</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין שום הבדל מעשי, שתי התיקיות שקולות זו לזו לחלוטין</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> <code>vars/</code> מכיל global variables/steps (כמו <code>vars/buildApp.groovy</code>) שנגישים ישירות מכל Jenkinsfile כפונקציה, ואילו <code>src/</code> מכיל Groovy classes מלאים (עם package, state ו-OOP) שנטענים באמצעות <code>import</code> - מתאים ללוגיקה מורכבת יותר.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>10. מה ההבדל בין Continuous Integration, Continuous Delivery ו-Continuous Deployment?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. שלושתם מונחים נרדפים לחלוטין לאותו תהליך בדיוק</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. CI הוא מיזוג קוד תדיר עם build/test אוטומטיים; Continuous Delivery - הקוד תמיד "ready to deploy" אך הפריסה עצמה דורשת אישור ידני; Continuous Deployment - כל שינוי שעובר את כל הבדיקות נפרס אוטומטית לפרודקשן ללא התערבות אנושית</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Continuous Deployment הוא שלב שקודם תמיד ל-Continuous Integration</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. Continuous Delivery דורש בהכרח שימוש ב-Kubernetes כדי לפעול</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> CI מתמקד במיזוג ובדיקה תדירים של קוד. Continuous Delivery מבטיח שהקוד תמיד מוכן לפריסה, אך הפריסה בפועל דורשת אישור אנושי (למשל דרך step של <code>input</code>). Continuous Deployment הולך צעד נוסף וכל שינוי שעובר בדיקות נפרס אוטומטית לפרודקשן, ללא התערבות ידנית.</div>
</div>

<button type="button" id="quiz-reset-all" class="quiz-reset-all">אפס את כל המבחן</button>

<style>
.quiz-score {
  font-weight: 600;
  font-size: 1.05em;
  padding: 0.6rem 1rem;
  margin-bottom: 1.5rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  border: 1px solid var(--sl-color-hairline);
  position: sticky;
  top: 0.5rem;
  z-index: 1;
}

.quiz-card {
  border: 1px solid var(--sl-color-hairline);
  border-radius: 10px;
  padding: 1rem 1.25rem;
  margin: 1.25rem 0;
  background: var(--sl-color-bg-nav);
}

.quiz-question {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.quiz-option {
  display: block;
  width: 100%;
  text-align: right;
  padding: 0.55rem 0.8rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
  line-height: 1.5;
  transition: background-color 0.15s ease, border-color 0.15s ease;
}

.quiz-option:hover:not(:disabled) {
  border-color: var(--sl-color-accent);
  background: var(--sl-color-gray-6);
}

.quiz-option:disabled {
  cursor: default;
}

.quiz-option--correct {
  border-color: var(--sl-color-green);
  background: color-mix(in srgb, var(--sl-color-green) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-option--incorrect {
  border-color: var(--sl-color-red);
  background: color-mix(in srgb, var(--sl-color-red) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-result {
  margin-top: 0.6rem;
  font-weight: 600;
  min-height: 1.2em;
}

.quiz-result--correct {
  color: var(--sl-color-green-high, var(--sl-color-green));
}

.quiz-result--incorrect {
  color: var(--sl-color-red-high, var(--sl-color-red));
}

.quiz-explain {
  display: none;
  margin-top: 0.6rem;
  padding: 0.7rem 0.9rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  font-size: 0.92em;
}

.quiz-card--answered .quiz-explain {
  display: block;
}

.quiz-reset-all {
  margin-top: 0.5rem;
  padding: 0.5rem 1.2rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg-nav);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
}

.quiz-reset-all:hover {
  border-color: var(--sl-color-accent);
}
</style>

<script>
(function () {
  function initQuiz() {
    var cards = document.querySelectorAll('.quiz-card');
    var scoreEl = document.getElementById('quiz-score');
    var resetAllBtn = document.getElementById('quiz-reset-all');

    function updateScore() {
      if (!scoreEl) return;
      var correctCount = document.querySelectorAll('.quiz-card--answered[data-was-correct="true"]').length;
      scoreEl.textContent = 'ענית נכון על ' + correctCount + ' מתוך ' + cards.length;
    }

    function answerCard(card, chosenBtn) {
      var correct = card.getAttribute('data-answer');
      var chosen = chosenBtn.getAttribute('data-choice');
      var isCorrect = chosen === correct;

      card.classList.add('quiz-card--answered');
      card.setAttribute('data-was-correct', isCorrect ? 'true' : 'false');

      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = true;
        if (opt.getAttribute('data-choice') === correct) {
          opt.classList.add('quiz-option--correct');
        } else if (opt === chosenBtn) {
          opt.classList.add('quiz-option--incorrect');
        }
      });

      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = isCorrect ? '✅ נכון!' : '❌ לא נכון - התשובה הנכונה מסומנת בירוק.';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
        result.classList.add(isCorrect ? 'quiz-result--correct' : 'quiz-result--incorrect');
      }

      updateScore();
    }

    function resetCard(card) {
      card.classList.remove('quiz-card--answered');
      card.removeAttribute('data-was-correct');
      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = false;
        opt.classList.remove('quiz-option--correct', 'quiz-option--incorrect');
      });
      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = '';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
      }
    }

    cards.forEach(function (card) {
      card.querySelectorAll('.quiz-option').forEach(function (btn) {
        btn.addEventListener('click', function () {
          if (card.classList.contains('quiz-card--answered')) return;
          answerCard(card, btn);
        });
      });
    });

    if (resetAllBtn) {
      resetAllBtn.addEventListener('click', function () {
        cards.forEach(resetCard);
        updateScore();
      });
    }

    updateScore();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initQuiz);
  } else {
    initQuiz();
  }
})();
</script>
