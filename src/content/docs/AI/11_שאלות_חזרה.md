---
title: "שאלות חזרה"
category: AI
part: 11/10
---

שאלות חזרה אינטראקטיביות על כל מה שלמדתם לאורך סדרת מדריכי ה-AI (מבוא לבינה מלאכותית, מודלי שפה, הנדסת פרומפטים, MCP, RAG, Fine-Tuning, AI Agents, LangChain ומסגרות עבודה, שאלות ראיון וטיפים וטריקים). בחרו תשובה בכל שאלה ותקבלו מיד משוב האם היא נכונה, יחד עם הסבר.

<div id="quiz-score" class="quiz-score">ענית נכון על 0 מתוך 10</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>1. מה קרה בשנת 2017 והניח את הבסיס הטכנולוגי לכל מודלי השפה הגדולים (LLM) של היום?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. השקת ChatGPT לציבור הרחב</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. פרסום המאמר <code>Attention Is All You Need</code> של גוגל, שהציג את ארכיטקטורת ה-Transformer</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. פריצת הדרך של AlexNet בתחרות ImageNet</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. כנס דארטמות' שבו נטבע המונח Artificial Intelligence</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> לפי ציר הזמן במדריך המבוא לבינה מלאכותית, ב-2017 פורסם המאמר "Attention Is All You Need" שהציג את ארכיטקטורת ה-Transformer, וזו הארכיטקטורה שעליה מבוססים כל מודלי השפה הגדולים (LLM) של היום. AlexNet ו-ImageNet שייכים ל-2012, וכנס דארטמות' וטבעית המונח AI היו כבר ב-1956.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>2. מהו ה-Context Window של מודל שפה גדול (LLM)?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. פרמטר ששולט ברמת האקראיות/יצירתיות של התשובה</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. כמות הטוקנים המקסימלית שהמודל "זוכר" במסגרת שיחה אחת</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. מספר השכבות (layers) של ה-Transformer במודל</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. גודל קובץ ה-checkpoint של המודל בדיסק</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> כפי שהוגדר בטבלת המושגים במדריך על LLM, Context Window הוא כמות הטוקנים המקסימלית שהמודל מסוגל "לזכור" בשיחה נתונה. שיחות שחורגות ממנו גורמות למודל "לשכוח" מידע ישן - בדיוק הסיבה שהמדריכים ממליצים לשים לב אליו.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>3. מהי טכניקת ה-Chain-of-Thought (CoT) בהנדסת פרומפטים?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. מתן מספר דוגמאות קונקרטיות (few-shot) לפורמט ולסגנון הרצויים</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. בקשה מהמודל "לחשוב בקול", שלב אחר שלב, לפני שהוא נותן תשובה סופית - משפרת דרמטית ביצועים במשימות הסקה מורכבות</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. הגדרת תפקיד או פרסונה למודל, כמו "אתה מומחה למיסים"</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. שימוש בתגיות XML כדי לתחם הוראות, טקסט ופורמט פלט</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך הנדסת הפרומפטים מציג את ה-Chain of Thought כבקשה מהמודל "לחשוב שלב אחר שלב לפני שהוא נותן תשובה סופית", ומדגיש שהטכניקה משפרת משמעותית ביצועים בבעיות מתמטיות, לוגיות וריבוי-שלבים - בשונה מ-Few-Shot (דוגמאות) או Role Prompting (הגדרת תפקיד).</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>4. איזו "בעיה" פותר פרוטוקול ה-MCP (Model Context Protocol), לפי מדריך ה-MCP?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. בעיית ה-Hallucination של מודלי שפה</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. בעיית האינטגרציה N×M - ריבוי חיבורים מותאמים-אישית בין מודלים לכלים - והפיכתה למודל M+N מתוקנן</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. בעיית ה-Overfitting בתהליכי אימון</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. בעיית גודל ה-Context Window המוגבל</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> לפני MCP, חיבור בין כל מודל לכל כלי דרש אינטגרציה ייעודית ("בעיית N×M"). MCP הופך זאת למודל M+N: כל כלי בונה שרת MCP אחד וכל אפליקציה בונה client אחד, וכולם "מדברים באותה שפה" - בדומה למה ש-USB-C עשה לחיבור מכשירים.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>5. מהו Hybrid Search במערכת RAG?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. שילוב בין שני מודלי LLM שונים לצורך יצירת תשובה אחת</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. שילוב חיפוש סמנטי (embeddings) עם חיפוש מילות מפתח קלאסי (BM25) לתוצאות אחזור מדויקות יותר</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. חיפוש שמתבצע גם באינטרנט הפתוח וגם במאגר וקטורי פנימי</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. שילוב בין Fine-Tuning ל-RAG על אותו מודל בו-זמנית</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך ה-RAG מגדיר Hybrid Search כשילוב של חיפוש סמנטי מבוסס embeddings עם חיפוש מילות מפתח קלאסי כמו BM25, במטרה לכסות טוב יותר גם מונחים מדויקים כמו שמות ומספרים שחיפוש סמנטי לבדו עלול לפספס.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>6. מהו ההבדל המרכזי בין Full Fine-tuning ל-LoRA?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. LoRA פועל רק על מודלים בקוד פתוח, ואילו Full Fine-tuning פועל על כל סוגי המודלים</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. Full Fine-tuning מעדכן את כל הפרמטרים של המודל, ואילו LoRA מקפיא את המשקלים המקוריים ומוסיף מטריצות קטנות "ניתנות אימון" בלבד, מה שחוסך משמעותית בזיכרון וב-GPU</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. LoRA מיועד רק לסיווג תמונות, ו-Full Fine-tuning רק לעיבוד טקסט</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין הבדל מעשי בין השניים, מדובר בשני שמות לאותה שיטה</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> כפי שמוסבר במדריך ה-Fine-Tuning, Full Fine-tuning מעדכן את כל משקלי המודל ודורש משאבים עצומים, בעוד ש-<code>LoRA (Low-Rank Adaptation)</code> מקפיא את המשקלים המקוריים ומוסיף לצדם מטריצות קטנות ניתנות-אימון בלבד - מה שמאפשר לשמור "adapters" קטנים במקום מודל שלם ולחסוך בזיכרון.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>7. מהו דפוס ה-ReAct (Reason + Act) בבניית סוכני AI?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. סוכן שמריץ תמיד את כל הכלים הזמינים לו במקביל, ובוחר את התוצאה הטובה ביותר</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. מעגל חוזר של "מחשבה" (Thought), "פעולה" (Action) באמצעות כלי, ו"תצפית" (Observation) על התוצאה, עד שהמודל מגיע לתשובה סופית</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. שיטת אימון מחדש (retraining) של הסוכן בזמן אמת תוך כדי שיחה</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. פרוטוקול תקשורת רשמי בין שני סוכני AI שונים, בדומה ל-MCP</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך ה-AI Agents מתאר את ReAct כמעגל חוזר של Thought (חשיבה) ← Action (פעולה, כמו קריאה לכלי) ← Observation (תצפית בתוצאה), שחוזר עד שיש מספיק מידע כדי לתת Final Answer - זהו אחד הדפוסים המוכרים ביותר לבניית סוכנים.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>8. מהו ההבדל המרכזי בין LangChain ל-LangGraph?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. LangChain רץ רק בפייתון, ואילו LangGraph זמין רק ב-TypeScript</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. LangChain מתמקד בשרשור לינארי (chains) ואינטגרציות, ואילו LangGraph מתמקד במבני state-machine מורכבים עם לולאות ותנאים, המתאימים במיוחד לבניית סוכנים</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. LangGraph הוא תחליף מלא ל-Claude Agent SDK של Anthropic</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. LangChain משמש אך ורק ל-RAG, ו-LangGraph משמש אך ורק ל-Fine-Tuning</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך LangChain ומסגרות העבודה מסביר ש-LangChain מתמקד בשרשור לינארי של קריאות LLM (chains) ואינטגרציות, בעוד LangGraph הוא הרחבה שלו המותאמת לבניית סוכנים מורכבים עם מבנה גרף (state machine), כולל לוגיקה מסועפת ולולאות.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>9. לפי מדריך שאלות הראיון, מתי חשוב Recall גבוה יותר מ-Precision גבוה, ולהפך?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. תמיד עדיף Precision גבוה, בכל תרחיש אפשרי, ללא יוצא מן הכלל</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. באבחון רפואי חשוב Recall גבוה (כדי לא לפספס חולים), ואילו בסינון ספאם חשוב Precision גבוה (כדי לא לחסום מיילים לגיטימיים)</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Recall ו-Precision הם שני שמות זהים לאותו מדד, ואין ביניהם trade-off</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. Recall רלוונטי רק ל-LLM, ואילו Precision רלוונטי רק למערכות Computer Vision</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך שאלות הראיון נותן בדיוק את הדוגמה הזו: באבחון רפואי מעדיפים Recall גבוה כדי לא לפספס חולים אמיתיים (גם במחיר של אזעקות שווא), ואילו בסינון ספאם מעדיפים Precision גבוה כדי לא לחסום בטעות מיילים לגיטימיים.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>10. מהי אחת הדרכים המומלצות לניהול עלויות (Cost Optimization) בעבודה עם LLM, לפי מדריך הטיפים והטריקים?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. להשתמש תמיד במודל ה"פרימיום" הגדול ביותר, ללא קשר למורכבות המשימה</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. לנצל Prompt Caching כשאותו הקשר ארוך (כמו system prompt או מסמכים) נשלח שוב ושוב - מה שחוסך עלות משמעותית</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. להימנע מהגבלת <code>max_tokens</code> כדי לקבל תמיד תשובות מלאות ומפורטות</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. להריץ תמיד את כל העיבוד בזמן אמת במקום להשתמש ב-Batch API</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> מדריך הטיפים והטריקים ממליץ, בין היתר, לבחור את גודל המודל הנכון למשימה, להגביל <code>max_tokens</code>, ולהשתמש ב-Batch API לעיבוד גדול - אך מדגיש במיוחד את Prompt Caching כדרך לחסוך עלות משמעותית כשאותו הקשר ארוך נשלח שוב ושוב.</div>
</div>

<button type="button" id="quiz-reset-all" class="quiz-reset-all">אפס את כל המבחן</button>

<style>
.quiz-score {
  font-weight: 600;
  font-size: 1.05em;
  padding: 0.6rem 1rem;
  margin-bottom: 1.5rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  border: 1px solid var(--sl-color-hairline);
  position: sticky;
  top: 0.5rem;
  z-index: 1;
}

.quiz-card {
  border: 1px solid var(--sl-color-hairline);
  border-radius: 10px;
  padding: 1rem 1.25rem;
  margin: 1.25rem 0;
  background: var(--sl-color-bg-nav);
}

.quiz-question {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.quiz-option {
  display: block;
  width: 100%;
  text-align: right;
  padding: 0.55rem 0.8rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
  line-height: 1.5;
  transition: background-color 0.15s ease, border-color 0.15s ease;
}

.quiz-option:hover:not(:disabled) {
  border-color: var(--sl-color-accent);
  background: var(--sl-color-gray-6);
}

.quiz-option:disabled {
  cursor: default;
}

.quiz-option--correct {
  border-color: var(--sl-color-green);
  background: color-mix(in srgb, var(--sl-color-green) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-option--incorrect {
  border-color: var(--sl-color-red);
  background: color-mix(in srgb, var(--sl-color-red) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-result {
  margin-top: 0.6rem;
  font-weight: 600;
  min-height: 1.2em;
}

.quiz-result--correct {
  color: var(--sl-color-green-high, var(--sl-color-green));
}

.quiz-result--incorrect {
  color: var(--sl-color-red-high, var(--sl-color-red));
}

.quiz-explain {
  display: none;
  margin-top: 0.6rem;
  padding: 0.7rem 0.9rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  font-size: 0.92em;
}

.quiz-card--answered .quiz-explain {
  display: block;
}

.quiz-reset-all {
  margin-top: 0.5rem;
  padding: 0.5rem 1.2rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg-nav);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
}

.quiz-reset-all:hover {
  border-color: var(--sl-color-accent);
}
</style>

<script>
(function () {
  function initQuiz() {
    var cards = document.querySelectorAll('.quiz-card');
    var scoreEl = document.getElementById('quiz-score');
    var resetAllBtn = document.getElementById('quiz-reset-all');

    function updateScore() {
      if (!scoreEl) return;
      var correctCount = document.querySelectorAll('.quiz-card--answered[data-was-correct="true"]').length;
      scoreEl.textContent = 'ענית נכון על ' + correctCount + ' מתוך ' + cards.length;
    }

    function answerCard(card, chosenBtn) {
      var correct = card.getAttribute('data-answer');
      var chosen = chosenBtn.getAttribute('data-choice');
      var isCorrect = chosen === correct;

      card.classList.add('quiz-card--answered');
      card.setAttribute('data-was-correct', isCorrect ? 'true' : 'false');

      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = true;
        if (opt.getAttribute('data-choice') === correct) {
          opt.classList.add('quiz-option--correct');
        } else if (opt === chosenBtn) {
          opt.classList.add('quiz-option--incorrect');
        }
      });

      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = isCorrect ? '✅ נכון!' : '❌ לא נכון - התשובה הנכונה מסומנת בירוק.';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
        result.classList.add(isCorrect ? 'quiz-result--correct' : 'quiz-result--incorrect');
      }

      updateScore();
    }

    function resetCard(card) {
      card.classList.remove('quiz-card--answered');
      card.removeAttribute('data-was-correct');
      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = false;
        opt.classList.remove('quiz-option--correct', 'quiz-option--incorrect');
      });
      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = '';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
      }
    }

    cards.forEach(function (card) {
      card.querySelectorAll('.quiz-option').forEach(function (btn) {
        btn.addEventListener('click', function () {
          if (card.classList.contains('quiz-card--answered')) return;
          answerCard(card, btn);
        });
      });
    });

    if (resetAllBtn) {
      resetAllBtn.addEventListener('click', function () {
        cards.forEach(resetCard);
        updateScore();
      });
    }

    updateScore();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initQuiz);
  } else {
    initQuiz();
  }
})();
</script>
