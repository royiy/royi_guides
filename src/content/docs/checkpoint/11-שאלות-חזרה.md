---
title: "שאלות חזרה"
category: Check Point
part: 11/10
---

שאלות חזרה אינטראקטיביות על כל מה שלמדתם לאורך מדריכי ה-Check Point (ארכיטקטורה והתקנה, מדיניות אבטחה ו-NAT, VPN, Identity Awareness, Threat Prevention, ניטור ולוגים, ClusterXL ופתרון תקלות). בחרו תשובה בכל שאלה ותקבלו מיד משוב האם היא נכונה, יחד עם הסבר.

<div id="quiz-score" class="quiz-score">ענית נכון על 0 מתוך 10</div>

<div class="quiz-card" data-answer="ג">
  <p class="quiz-question"><strong>1. מהו תפקידו של Security Management Server (SMS) בארכיטקטורת שלוש השכבות של Check Point?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. תוכנת ה-GUI (SmartConsole) שדרכה מנהל המערכת בונה מדיניות</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. השרת שאוכף בפועל את המדיניות בתעבורת הרשת (Inspection, NAT, VPN)</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. "המוח" של המערכת - שומר את מסד הנתונים, המדיניות והלוגים, ומבצע Compile ו-Install Policy לגייטווי</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. מערכת ההפעלה הייעודית (Gaia) שעליה רצים כל הרכיבים</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> ב-Three-Tier Architecture, ה-SMS הוא הרכיב המרכזי שמנהל את כל התצורה: הוא שומר את מסד הנתונים והמדיניות, מריץ Compile ו-Install Policy לגייטווי, ומרכז את הלוגים. SmartConsole הוא רק ה-GUI לניהול, וה-Security Gateway הוא זה שאוכף בפועל.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>2. מדוע סנכרון שעונים (NTP) קריטי כל כך בתהליך ההקמה וה-SIC של Check Point?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. שעון לא מסונכרן בין ה-SMS לגייטווי גורם לבעיות ב-SIC, בלוגים וב-VPN</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. NTP נדרש רק כדי להציג את השעה הנכונה במסך הכניסה</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. בלי NTP לא ניתן להתקין את SmartConsole על מחשב Windows</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. NTP מחליף את הצורך ב-Activation Key בהגדרת SIC</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> ה-First Time Configuration Wizard מדגיש את חשיבות ה-NTP - שעון לא מסונכרן גורם לבעיות SIC, לוגים ו-VPN. גם בפתרון תקלות SIC, בדיקת השעון היא אחד הצעדים הראשונים המומלצים, עוד לפני בדיקת חסימת פורטים או Activation Key.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>3. מהו ה-Cleanup Rule ב-Rule Base של Check Point?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. כלל ראשון בטבלה שחוסם גישה ישירה לגייטווי עצמו (זהו Stealth Rule)</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. הכלל האחרון בטבלה, בד"כ <code>Any/Any/Any -> Drop</code> עם Log, שתופס כל תעבורה שלא הותאמה לאף כלל קודם (Deny by Default)</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. כלל שמבצע Threat Extraction אוטומטי על קבצים</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. כלל שמוחק אוטומטית כללים לא בשימוש לפי Hit Count</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Cleanup Rule הוא הכלל האחרון ב-Rule Base, שתופס כל מה שלא הותאם לכללים הקודמים ומבצע לו Drop עם Log - מימוש ישיר של עקרון ה-Deny by Default. זה שונה מ-Stealth Rule, שתפקידו לחסום גישה ישירה לגייטווי עצמו.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>4. מה ההבדל בין Phase 1 ל-Phase 2 בפרוטוקול IKE ב-VPN?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. Phase 1 מצפין את התעבורה בפועל; Phase 2 רק מאמת את הגייטווים</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. Phase 1 יוצר Secure Channel מאומת בין הגייטווים (Pre-Shared Key או Certificate); Phase 2 מקים את ה-IPsec SA שמצפין את התעבורה עצמה</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Phase 1 רלוונטי רק ל-Remote Access VPN; Phase 2 רק ל-Site-to-Site VPN</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין הבדל מהותי בין השלבים, שניהם מתבצעים תמיד יחד</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> IKE מתבצע בשני שלבים: Phase 1 מקים ערוץ מאובטח בין הגייטווים תוך אימות (Pre-Shared Key/Certificate) והצפנה; Phase 2 מקים את ה-IPsec SA שבפועל מצפין את תעבורת המשתמשים. בטיפול בתקלות VPN תמיד בודקים קודם את Phase 1, כי אם הוא לא עובד אין טעם לבדוק את Phase 2.</div>
</div>

<div class="quiz-card" data-answer="ד">
  <p class="quiz-question"><strong>5. מה ההבדל בין AD Query ל-Identity Agent כשיטות זיהוי ב-Identity Awareness?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. AD Query דורש התקנת Agent על כל תחנת קצה; Identity Agent הוא Agentless לחלוטין</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. שתי השיטות זהות ואין ביניהן הבדל מעשי</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. AD Query מתאים רק לסביבות Terminal Server משותפות</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. AD Query הוא Agentless וקורא את לוגי האבטחה של Domain Controller-ים; Identity Agent הוא תוכנה שמותקנת בתחנת הקצה ומדווחת זהות ישירות וביתר דיוק</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> AD Query היא השיטה הנפוצה ביותר, Agentless - הגייטווי קורא את Security Event Logs של ה-Domain Controllers. Identity Agent, לעומת זאת, הוא תוכנה שמותקנת בתחנת הקצה ומדווחת זהות ישירות לגייטווי - מדויק יותר, ונדרש בפרט בסביבות עם Terminal Servers שבהן כמה משתמשים חולקים אותו IP.</div>
</div>

<div class="quiz-card" data-answer="ד">
  <p class="quiz-question"><strong>6. מה ההבדל בין Threat Emulation ל-Threat Extraction?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. Threat Emulation מנקה קבצים ממאקרואים; Threat Extraction מריץ אותם בסביבת Sandbox</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. שתי הטכנולוגיות פועלות רק על תעבורת HTTPS מוצפנת</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Threat Extraction הוא שם ישן ל-Threat Emulation מגרסאות קודמות</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. Threat Emulation מריץ קובץ חשוד בסביבת Sandbox מבודדת לזיהוי התנהגות זדונית לא ידועה (Zero-Day); Threat Extraction מנקה (CDR) את הקובץ ממאקרואים ואובייקטים פעילים ומעביר גרסה נקייה מיידית</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> Threat Emulation "תופס" תוכנה זדונית לא מוכרת על ידי הרצת הקובץ ב-Sandbox ובדיקת התנהגותו. Threat Extraction (CDR - Content Disarm & Reconstruction) לא מנסה לתפוס תוכנה זדונית, אלא פשוט מנקה את הקובץ מאלמנטים פעילים (מאקרו, קישורים) ומעביר גרסה נקייה מיידית, בעוד המקור נבדק ברקע.</div>
</div>

<div class="quiz-card" data-answer="ג">
  <p class="quiz-question"><strong>7. מה מציגה הפקודה <code>fw ctl pstat</code> בגייטווי, ולמה היא חשובה לניטור?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. רק את גרסת חתימות ה-IPS המותקנת</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. את רשימת המשתמשים המחוברים דרך Identity Awareness</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. מידע על Kernel Memory, Connections Table Usage, Cookies ו-Fragments - חשוב לזיהוי בעיות ביצועים או מתקפת DoS</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. את תוכן קובץ ה-Rule Base הנוכחי</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> <code>fw ctl pstat</code> הוא אחד הכלים החשובים ביותר לניטור בריאות הגייטווי - הוא מציג מידע על Kernel Memory, ניצול טבלת החיבורים (Connections Table), Cookies ו-Fragments. תפוקה גבוהה בטבלת ה-Connections קרוב ל-Limit היא סימן אזהרה מוקדם לבעיות ביצועים או מתקפת DoS.</div>
</div>

<div class="quiz-card" data-answer="א">
  <p class="quiz-question"><strong>8. מה ההבדל בין High Availability (HA) Mode ל-Load Sharing Mode ב-ClusterXL?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. ב-HA Mode חבר אחד Active והשני Standby עד Failover; ב-Load Sharing שני החברים פעילים בו-זמנית וחולקים עומס</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. ב-HA Mode שני החברים פעילים בו-זמנית; ב-Load Sharing רק חבר אחד פעיל</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. Load Sharing זמין רק בגרסת Gaia הישנה ואינו נתמך היום</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין הבדל מעשי בין שני המצבים</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> ב-HA Mode חבר אחד פעיל (Active) והשני בהמתנה (Standby), ועובר להיות Active תוך שניות בודדות במקרה של Failover. ב-Load Sharing Mode שני החברים פעילים בו-זמנית ומחלקים ביניהם את העומס (Multicast/Unicast). ברוב הארגונים בוחרים ב-HA לפשטות ואמינות.</div>
</div>

<div class="quiz-card" data-answer="ג">
  <p class="quiz-question"><strong>9. ב-<code>fw monitor</code>, מה המשמעות אם חבילה מופיעה בנקודת הבדיקה <code>i</code> אך לא מופיעה ב-<code>I</code>?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. החבילה עברה בהצלחה דרך כל שרשרת ה-Kernel</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. מדובר בתעבורת VPN מוצפנת שלא ניתן לנטר בכלל</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. החבילה נחסמה על ידי ה-Firewall - היא נכנסה (<code>i</code>) אך לא עברה את שלב ה-Inspection (<code>I</code>)</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. יש בעיה בממשק הפיזי (Layer 1) בלבד</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> ל-<code>fw monitor</code> יש ארבע נקודות אינספקציה לאורך ה-Kernel Chain: <code>i</code> (כניסה לפני Inspection), <code>I</code> (כניסה אחרי Inspection), <code>o</code> (יציאה לפני Inspection) ו-<code>O</code> (יציאה אחרי Inspection). אם חבילה מופיעה ב-<code>i</code> אך לא ב-<code>I</code>, זהו סימן ברור שהיא נחסמה על ידי ה-Firewall בשלב ה-Inspection.</div>
</div>

<div class="quiz-card" data-answer="ב">
  <p class="quiz-question"><strong>10. מהו סדר ההסמכות (Certifications) הרשמי של Check Point, מהבסיסי ביותר למתקדם ביותר?</strong></p>
  <div class="quiz-options">
    <button class="quiz-option" type="button" data-choice="א">א. CCSE -> CCSA -> CCSM -> CCSM Elite</button>
    <button class="quiz-option" type="button" data-choice="ב">ב. CCSA -> CCSE -> CCSM -> CCSM Elite</button>
    <button class="quiz-option" type="button" data-choice="ג">ג. CCSM -> CCSE -> CCSA -> CCSM Elite</button>
    <button class="quiz-option" type="button" data-choice="ד">ד. אין סדר מוגדר - כל ההסמכות נחשבות לאותה רמה</button>
  </div>
  <div class="quiz-result" aria-live="polite"></div>
  <div class="quiz-explain"><strong>הסבר:</strong> הסדר הרשמי הוא CCSA (Security Administrator - רמת בסיס, ניהול יומיומי), ואז CCSE (Security Expert - רמה מתקדמת, Troubleshooting, VPN, Cluster), ואז CCSM (Security Master - רמת מומחה אחרי CCSE + Specializations), ובראש הסולם CCSM Elite.</div>
</div>

<button type="button" id="quiz-reset-all" class="quiz-reset-all">אפס את כל המבחן</button>

<style>
.quiz-score {
  font-weight: 600;
  font-size: 1.05em;
  padding: 0.6rem 1rem;
  margin-bottom: 1.5rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  border: 1px solid var(--sl-color-hairline);
  position: sticky;
  top: 0.5rem;
  z-index: 1;
}

.quiz-card {
  border: 1px solid var(--sl-color-hairline);
  border-radius: 10px;
  padding: 1rem 1.25rem;
  margin: 1.25rem 0;
  background: var(--sl-color-bg-nav);
}

.quiz-question {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.quiz-option {
  display: block;
  width: 100%;
  text-align: right;
  padding: 0.55rem 0.8rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
  line-height: 1.5;
  transition: background-color 0.15s ease, border-color 0.15s ease;
}

.quiz-option:hover:not(:disabled) {
  border-color: var(--sl-color-accent);
  background: var(--sl-color-gray-6);
}

.quiz-option:disabled {
  cursor: default;
}

.quiz-option--correct {
  border-color: var(--sl-color-green);
  background: color-mix(in srgb, var(--sl-color-green) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-option--incorrect {
  border-color: var(--sl-color-red);
  background: color-mix(in srgb, var(--sl-color-red) 18%, var(--sl-color-bg));
  color: var(--sl-color-text);
}

.quiz-result {
  margin-top: 0.6rem;
  font-weight: 600;
  min-height: 1.2em;
}

.quiz-result--correct {
  color: var(--sl-color-green-high, var(--sl-color-green));
}

.quiz-result--incorrect {
  color: var(--sl-color-red-high, var(--sl-color-red));
}

.quiz-explain {
  display: none;
  margin-top: 0.6rem;
  padding: 0.7rem 0.9rem;
  border-radius: 8px;
  background: var(--sl-color-gray-6);
  font-size: 0.92em;
}

.quiz-card--answered .quiz-explain {
  display: block;
}

.quiz-reset-all {
  margin-top: 0.5rem;
  padding: 0.5rem 1.2rem;
  border-radius: 8px;
  border: 1px solid var(--sl-color-hairline);
  background: var(--sl-color-bg-nav);
  color: var(--sl-color-text);
  cursor: pointer;
  font: inherit;
}

.quiz-reset-all:hover {
  border-color: var(--sl-color-accent);
}
</style>

<script>
(function () {
  function initQuiz() {
    var cards = document.querySelectorAll('.quiz-card');
    var scoreEl = document.getElementById('quiz-score');
    var resetAllBtn = document.getElementById('quiz-reset-all');

    function updateScore() {
      if (!scoreEl) return;
      var correctCount = document.querySelectorAll('.quiz-card--answered[data-was-correct="true"]').length;
      scoreEl.textContent = 'ענית נכון על ' + correctCount + ' מתוך ' + cards.length;
    }

    function answerCard(card, chosenBtn) {
      var correct = card.getAttribute('data-answer');
      var chosen = chosenBtn.getAttribute('data-choice');
      var isCorrect = chosen === correct;

      card.classList.add('quiz-card--answered');
      card.setAttribute('data-was-correct', isCorrect ? 'true' : 'false');

      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = true;
        if (opt.getAttribute('data-choice') === correct) {
          opt.classList.add('quiz-option--correct');
        } else if (opt === chosenBtn) {
          opt.classList.add('quiz-option--incorrect');
        }
      });

      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = isCorrect ? '✅ נכון!' : '❌ לא נכון - התשובה הנכונה מסומנת בירוק.';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
        result.classList.add(isCorrect ? 'quiz-result--correct' : 'quiz-result--incorrect');
      }

      updateScore();
    }

    function resetCard(card) {
      card.classList.remove('quiz-card--answered');
      card.removeAttribute('data-was-correct');
      var options = card.querySelectorAll('.quiz-option');
      options.forEach(function (opt) {
        opt.disabled = false;
        opt.classList.remove('quiz-option--correct', 'quiz-option--incorrect');
      });
      var result = card.querySelector('.quiz-result');
      if (result) {
        result.textContent = '';
        result.classList.remove('quiz-result--correct', 'quiz-result--incorrect');
      }
    }

    cards.forEach(function (card) {
      card.querySelectorAll('.quiz-option').forEach(function (btn) {
        btn.addEventListener('click', function () {
          if (card.classList.contains('quiz-card--answered')) return;
          answerCard(card, btn);
        });
      });
    });

    if (resetAllBtn) {
      resetAllBtn.addEventListener('click', function () {
        cards.forEach(resetCard);
        updateScore();
      });
    }

    updateScore();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initQuiz);
  } else {
    initQuiz();
  }
})();
</script>
